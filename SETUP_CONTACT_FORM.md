# F-tecお問い合わせフォーム：Google Apps Script設定手順

この一式では、フォームの送信ボタンを押すと、メールアプリを開かずにGoogle Apps Scriptへ直接POSTし、`university.of.tokyo.ftec@gmail.com`へメールを送ります。

## 先に確認

- Google Apps Scriptは、できるだけ `university.of.tokyo.ftec@gmail.com` にログインした状態で作成・デプロイしてください。
- フォームからの通知メールには、送信者のメールアドレスを `Reply-To` として設定しています。F-tec側で受信メールへ返信すると、そのままお問い合わせ者へ返信できます。
- お問い合わせ者にも受付番号付きの自動返信を送ります。

## 1. Apps Scriptを作る

1. ブラウザで `https://script.google.com/` を開きます。
2. 「新しいプロジェクト」を押します。
3. プロジェクト名を `F-tec お問い合わせフォーム` にします。
4. 初期表示の `Code.gs` を全削除し、このフォルダの `apps-script/Code.gs` を全文貼り付けます。
5. 左側の「プロジェクトの設定」でタイムゾーンを `GMT+09:00 東京` にします。
6. 保存します。

`appsscript.json` を表示・編集できる場合は、同梱の `apps-script/appsscript.json` と同じ内容にしてください。表示できなくてもCode.gsだけで動作します。

## 2. ウェブアプリとしてデプロイ

1. 右上の「デプロイ」→「新しいデプロイ」。
2. 種類は「ウェブアプリ」。
3. 「次のユーザーとして実行」は **自分**。
4. 「アクセスできるユーザー」は **Googleにログインしていない人も利用できる全員** を選択します。画面上では「全員」「Anyone」等と表示される場合があります。
5. 「デプロイ」を押し、Googleの権限確認を完了します。
6. 表示された **ウェブアプリURL（末尾 `/exec`）** をコピーします。`/dev` URLは使用しません。

## 3. サイトにURLを設定

この一式を `ftec-official` フォルダ直下へ上書きした後、

1. `set-apps-script-url.bat` をダブルクリックします。
2. コピーした `/exec` URLを貼り付け、Enter。
3. 「設定完了」と表示されたら終了です。

設定確認は、PowerShellで `verify-contact-form.ps1` を実行します。

## 4. 非公開のままテスト

1. VS CodeのLive Serverで `/contact/` を開きます。
2. 自分の別メールアドレスを入力してテスト送信します。
3. ブラウザに「送信が完了しました」と受付番号が表示されることを確認します。
4. `university.of.tokyo.ftec@gmail.com` に通知メールが届くことを確認します。
5. 入力した別メールアドレスに自動返信が届くことを確認します。
6. F-tec側の通知メールで「返信」を押し、宛先がお問い合わせ者になっていることを確認します。

## 5. GitHubへ保存

GitHub Desktopで、

1. Summaryに `お問い合わせフォームを自動送信方式に変更` と入力。
2. `Commit to main`。
3. `Push origin`。

リポジトリがPrivateで、Vercel・GitHub Pagesに接続していなければ、一般公開はされません。

## 更新時の注意

Apps Scriptのコードを変更した場合は、保存だけでなく「デプロイを管理」から新しいバージョンを反映してください。URLを変えずに更新できます。

## 迷惑メール対策

- 画面に表示されないハニーポット欄
- サーバー側の入力値検証と文字数制限
- 同一内容の短時間二重送信防止
- HTMLエスケープ

を実装しています。大量の迷惑投稿が発生した場合は、Turnstile等の追加対策が必要です。
